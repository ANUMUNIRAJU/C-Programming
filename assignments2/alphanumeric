#include <stdio.h>
#include<string.h>
int main(void)
{
	int i,c=0,b=0;
	char a[10];
	printf("enter the string");
	gets(a);
	int n=strlen(a);
	for(i=0;i<n;i++)
	{
		if((a[i]>=65&&a[i]<=96)||(a[i]>=97&&a[i]<=122))
		{
			c++;
		}
	else
	b++;
	}
	printf("%d,%d",c,b);
	return 0;
}
